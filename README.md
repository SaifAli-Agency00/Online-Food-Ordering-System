# Online-Food-Ordering-System

The complete project is inside the `online-food-ordering/` folder.

See `online-food-ordering/README.md` for setup instructions, SQL import steps, admin credentials, and troubleshooting.

Deployment notes are in `online-food-ordering/DEPLOYMENT.md`.
